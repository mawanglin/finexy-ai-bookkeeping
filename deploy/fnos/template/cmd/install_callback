#!/bin/bash
# After files are installed: generate the runtime env file from wizard values.
. "$(dirname "$0")/functions"
# On upgrade the wizard may not be shown; keep the existing config.
if [ ! -f "$ENV_FILE" ] || [ -n "${wizard_port}" ]; then
    write_env
fi
exit 0
