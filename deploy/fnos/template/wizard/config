[
  {
    "stepTitle": "Finexy 设置（保存后自动重启生效）",
    "items": [
      {
        "type": "tips",
        "helpText": "修改后需重启应用生效。DeepSeek API Key 留空表示保持当前值（无法在此处清除）；如需关闭 AI 记账，请编辑应用数据目录中的 finexy.env。"
      },
      {
        "type": "text",
        "field": "wizard_port",
        "label": "服务端口",
        "initValue": "8080",
        "rules": [
          {
            "required": true,
            "message": "请输入端口"
          },
          {
            "pattern": "^[0-9]{2,5}$",
            "message": "请输入 1024-65535 之间的端口"
          }
        ]
      },
      {
        "type": "text",
        "field": "wizard_external_url",
        "label": "外部访问地址（可选）",
        "initValue": "",
        "rules": [
          {
            "required": false
          }
        ]
      },
      {
        "type": "password",
        "field": "wizard_deepseek_api_key",
        "label": "DeepSeek API Key（留空保持当前值）",
        "initValue": "",
        "rules": [
          {
            "required": false
          }
        ]
      },
      {
        "type": "text",
        "field": "wizard_deepseek_model",
        "label": "模型 ID",
        "initValue": "deepseek-v4-flash",
        "rules": [
          {
            "required": false
          }
        ]
      }
    ]
  }
]